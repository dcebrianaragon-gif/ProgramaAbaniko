{
  "name": "programa-abaniko",
  "version": "1.0.0",
  "private": true,
  "description": "Programa Abaniko con backend local, Netlify y sincronizacion Supabase.",
  "scripts": {
    "start": "node backend/server.js",
    "build": "node tools/build-netlify.js",
    "deploy": "npm run build && npx netlify-cli deploy --prod --dir dist",
    "deploy:netlify": "npm run build && npx netlify-cli deploy --prod --dir dist",
    "deploy:vercel": "npm run build && npx vercel deploy --prod"
  }
}
